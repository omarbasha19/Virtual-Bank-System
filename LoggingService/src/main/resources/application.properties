# Server Configuration
server.port=9095

# Spring Application Name
spring.application.name=LoggingService

# Kafka Consumer Configuration
spring.kafka.consumer.bootstrap-servers=localhost:9092
spring.kafka.consumer.group-id=vbank-log-group
spring.kafka.consumer.auto-offset-reset=earliest
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.apache.kafka.common.serialization.StringDeserializer

# MySQL Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/log_service_db
spring.datasource.username=Ejada-Virtual-Banking
spring.datasource.password=Omar2@Alimobarak
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect